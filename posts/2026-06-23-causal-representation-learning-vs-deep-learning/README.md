# Internal Notes

This draft post was derived from the research note:

```text
/Users/lancelotdacosta/Desktop/Work/1_RESEARCH/12_Studio/crl_vs_deep_learning.md
```

The editable blog draft is:

```text
index.qmd
```

The post is currently marked as a Quarto draft with:

```yaml
draft: true
```

While this flag is present, Quarto should exclude the post from the rendered site listing and public output. Remove `draft: true` when the post is ready to publish.

The draft intentionally includes `[Flag: ...]` markers where claims need a better source, a softer phrasing, or the author's judgment before publication.
